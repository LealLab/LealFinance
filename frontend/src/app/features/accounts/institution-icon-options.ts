import { IconName } from '../../shared/ui/icon/icon';

/** Curated subset of the icon set that makes sense as an institution glyph. */
export const INSTITUTION_ICON_OPTIONS: readonly IconName[] = ['bank', 'wallet', 'chart', 'globe', 'archive'];

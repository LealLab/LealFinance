Alembic migrations for the LealFinance backend live in this directory.

Run migration commands from the repository root. See
[`docs/development.md`](../../docs/development.md#migrations) for the workflow,
including `task backend:migrate` and
`task backend:migration -- "message"`.

Alembic migrations for the LealFinance backend live in this directory.

Run migration commands from the repository root with the Taskfile commands
documented in [`docs/development.md`](../../docs/development.md), especially
`task backend:migrate` and `task backend:migration -- "message"`.
